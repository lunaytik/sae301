const progressBar=document.getElementById("progress-bar");const steps=document.querySelectorAll(".step");let active=1;const updateProgress=()=>{steps.forEach((step,i)=>{if(i<active){step.classList.add("active")}else{step.classList.remove("active")}});progressBar.style.width=((active-1)/(steps.length-1))*100+"%"};